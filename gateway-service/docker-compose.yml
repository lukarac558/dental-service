version: "3"
services:
  gateway-service:
    image: gateway-service
    container_name: gateway-service
    build:
      context: ../gateway-service
      dockerfile: Dockerfile
    ports:
      - "8080:8080"